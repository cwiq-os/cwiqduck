# name: test/sql/cwiq.test
# description: test cwiq extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT cwiq('Sam');
----
Catalog Error: Scalar Function with name cwiq does not exist!

# Require statement will ensure this test is run with this extension loaded
require cwiq

# Confirm the extension works
query I
SELECT cwiq('Sam');
----
Cwiq Sam 🐥

query I
SELECT cwiq_openssl_version('Michael') ILIKE 'Cwiq Michael, my linked OpenSSL version is OpenSSL%';
----
true
